# Silentridge boilerplate for UI with tailwind

## Features
- Custom tailwind css
- Live server
- Live reload

## Usage
- `yarn install`
- `yarn serve` -  build all and start local server with live reload
- `yarn build` -  Only rebuild project without starting server